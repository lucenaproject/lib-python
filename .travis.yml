#sudo: required
#
#services:
#  - docker
#
#script:
#  - docker build --tag lucena-lib-python .
#  - docker run lucena-lib-python python -m unittest tests/*.py
#
#notifications:
#  on_success: never

language: python
python:
  - "3.6"
install:
  - pip install python-coveralls
  - pip install coverage
  - pip install nose 
script:
  - pytest
after_success:
  - coveralls
